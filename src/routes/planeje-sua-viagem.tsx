import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/planeje-sua-viagem")({
  component: () => <div className="p-24 text-center">Página Planeje sua Viagem em construção</div>,
});
