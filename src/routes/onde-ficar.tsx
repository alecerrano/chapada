import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/onde-ficar")({
  component: () => <div className="p-24 text-center">Página Onde Ficar em construção</div>,
});
