import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/cavalcante")({
  component: () => <div className="p-24 text-center">Página Cavalcante em construção</div>,
});
