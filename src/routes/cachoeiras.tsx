import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/cachoeiras")({
  component: () => <div className="p-24 text-center">Página Cachoeiras em construção</div>,
});
