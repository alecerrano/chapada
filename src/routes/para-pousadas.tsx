import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/para-pousadas")({
  component: () => <div className="p-24 text-center">Página Para Pousadas em construção</div>,
});
