import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/alto-paraiso")({
  component: () => <div className="p-24 text-center">Página Alto Paraíso em construção</div>,
});
