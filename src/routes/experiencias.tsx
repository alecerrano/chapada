import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/experiencias")({
  component: () => <div className="p-24 text-center">Página Experiências em construção</div>,
});
